name: Basic Pallet
slug: basic-pallet
version: 0.1.0  # Semantic version for this recipe
category: polkadot-sdk-cookbook
needs_node: false
description: Replace with a short description.
type: polkadot-sdk

# Metadata for documentation
difficulty: beginner  # beginner | intermediate | advanced
docs_ready: false  # Ready to be referenced in docs.polkadot.com?
tags:
  - pallet
  - runtime
  - substrate

# Polkadot SDK versions (optional overrides)
# Uncomment and modify if you need specific versions
# polkadot_sdk: "1.15.0"
# rust: "1.81.0"

# Test configuration
test_command: just test
build_command: just build
lint_command: just clippy

# Optional: Related recipes
# related_recipes:
#   - another-recipe-slug

# Optional: External resources
# resources:
#   - title: "FRAME Documentation"
#     url: "https://docs.substrate.io/reference/frame-pallets/"
