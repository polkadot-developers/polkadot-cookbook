name: REVM OpenZeppelin Contracts

on:
  push:
    branches: [master]
    paths:
      - 'migration/revm/openzeppelin-contracts/**'
      - 'versions.yml'
  pull_request:
    paths:
      - 'migration/revm/openzeppelin-contracts/**'
      - 'versions.yml'
  workflow_dispatch:

jobs:
  test:
    if: github.repository == 'polkadot-developers/polkadot-cookbook'
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - name: Load versions
        id: versions
        run: |
          echo "POLKADOT_SDK_VERSION=$(yq '.polkadot_sdk.release_tag' versions.yml)" >> $GITHUB_OUTPUT

      - uses: actions/setup-node@v4
        with:
          node-version: '22'

      - name: Setup pallet-revive dev node
        uses: ./.github/actions/setup-revive-dev-node
        with:
          polkadot-sdk-version: ${{ steps.versions.outputs.POLKADOT_SDK_VERSION }}

      - name: Install npm dependencies
        run: |
          cd migration/revm/openzeppelin-contracts
          npm ci

      - name: Run tests
        run: |
          cd migration/revm/openzeppelin-contracts
          npm test
        timeout-minutes: 30

  post-test:
    needs: test
    if: github.repository == 'polkadot-developers/polkadot-cookbook' && always() && github.ref == 'refs/heads/master'
    uses: ./.github/workflows/post-cleanup.yml
    with:
      test_result: ${{ needs.test.result }}
      readme_path: 'migration/revm/openzeppelin-contracts/README.md'
      workflow_name: 'REVM OpenZeppelin Contracts'
    permissions:
      contents: read
      issues: write
