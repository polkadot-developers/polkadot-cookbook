## Zero to Hero Tutorial Harness

This tutorial mirrors the Zero‑to‑Hero flow. It uses the kitchensink runtime for local runs.

Quick start:

```
just build
just chain-spec
just start-dev
```

Or run with a local relay using Zombienet:

```
just spawn-omni
```


