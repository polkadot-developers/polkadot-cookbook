<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 1200 200" shape-rendering="geometricPrecision"
     role="img" aria-label="{{PATHWAY_LABEL}} pathway — {{PATHWAY_COUNT}} recipes">
  <!-- Generated by .claude/skills/branding/generate.py. DO NOT EDIT.
       v2: 3-column (number + body + per-pathway glyph). -->

  <rect width="1200" height="200" fill="{{CANVAS}}"/>

  <!-- Col 1: Number -->
  <rect x="0" y="0" width="100" height="200" fill="{{SURFACE}}"/>
  <text x="50" y="110" text-anchor="middle" font-family="{{MONO}}" font-size="40" font-weight="800"
        fill="{{PINK}}" letter-spacing="-2">{{PATHWAY_NUMBER}}</text>
  <text x="50" y="140" text-anchor="middle" font-family="{{MONO}}" font-size="9" fill="{{FG_DIM}}"
        letter-spacing="1.5">RECIPES</text>
  <line x1="100" y1="0" x2="100" y2="200" stroke="{{LINE}}"/>

  <!-- Col 2: Body -->
  <text x="130" y="60" font-family="{{MONO}}" font-size="10" fill="{{FG_DIM}}" letter-spacing="2">PATHWAY · {{PATHWAY_NUMBER}}/05</text>
  <text x="130" y="110" font-family="{{MONO}}" font-size="56" font-weight="700" fill="{{FG}}" letter-spacing="-1">{{PATHWAY_LABEL}}</text>
  <text x="130" y="145" font-family="{{MONO}}" font-size="11" fill="{{FG_MUTED}}" letter-spacing="1">{{PATHWAY_TAGLINE}}</text>

  <!-- Col 3: Glyph -->
  <line x1="940" y1="0" x2="940" y2="200" stroke="{{LINE}}"/>
  <rect x="940" y="0" width="260" height="200" fill="{{SURFACE}}"/>
  {{PATHWAY_GLYPH}}
</svg>
