<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 1200 12" role="presentation" aria-hidden="true">
  <!-- Generated. DO NOT EDIT.
       Strict 3-color divider: pink → black/white → pink gradient flowing. -->
  <style>
    @media (prefers-reduced-motion: reduce) {
      .flow { animation: none !important; }
    }
    .flow {
      animation: flow {{GRADIENT_FLOW_DUR}} linear infinite;
    }
    @keyframes flow {
      0%   { transform: translateX(0); }
      100% { transform: translateX(-600px); }
    }
  </style>
  <defs>
    <linearGradient id="g" x1="0" y1="0" x2="1" y2="0">
      <stop offset="0"    stop-color="{{PINK}}"/>
      <stop offset="0.5"  stop-color="{{FG}}"/>
      <stop offset="1"    stop-color="{{PINK}}"/>
    </linearGradient>
  </defs>
  <rect width="1200" height="12" fill="{{CANVAS}}"/>
  <g class="flow">
    <rect x="0" y="4" width="1800" height="4" fill="url(#g)"/>
  </g>
</svg>
