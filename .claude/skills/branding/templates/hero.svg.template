<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 1200 630" shape-rendering="crispEdges" role="img" aria-label="Polkadot Cookbook — Practical, tested recipes for building on Polkadot.">
  <!-- Generated by .claude/skills/branding/generate.sh. DO NOT EDIT.
       Evergreen and static: no version, no counts, no animations.
       Rasterizes cleanly to PNG for social-card use. -->
  <rect width="1200" height="630" fill="{{CANVAS}}"/>

  <!-- Pink identity slab -->
  <rect x="0" y="0" width="1200" height="540" fill="{{PINK}}"/>
  <text x="80" y="230" font-family="{{MONO}}" font-size="108" font-weight="700" fill="{{TERMINAL}}" letter-spacing="-2">POLKADOT</text>
  <text x="80" y="340" font-family="{{MONO}}" font-size="108" font-weight="700" fill="{{TERMINAL}}" letter-spacing="-2">COOKBOOK</text>
  <rect x="80" y="368" width="460" height="4" fill="{{TERMINAL}}"/>
  <text x="80" y="414" font-family="{{MONO}}" font-size="22" fill="{{TERMINAL}}" opacity="0.9">Practical, tested recipes for building on Polkadot.</text>

  <!-- Blue accent bar -->
  <rect x="1080" y="0" width="120" height="540" fill="{{BLUE}}"/>

  <!-- Terminal footer -->
  <rect x="0" y="540" width="1200" height="90" fill="{{TERMINAL}}"/>
  <text x="80" y="585" font-family="{{MONO}}" font-size="15" fill="{{CREAM}}" opacity="0.7" letter-spacing="3">PALLETS · CONTRACTS · TRANSACTIONS · XCM · NETWORKS</text>
  <text x="80" y="612" font-family="{{MONO}}" font-size="12" fill="{{CREAM}}" opacity="0.4">github.com/polkadot-developers/polkadot-cookbook</text>
  <text x="1120" y="612" font-family="{{MONO}}" font-size="11" fill="{{CREAM}}" opacity="0.4" text-anchor="end">MIT / Apache-2.0</text>
</svg>
