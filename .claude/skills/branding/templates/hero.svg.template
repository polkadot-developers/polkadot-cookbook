<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 1200 630" shape-rendering="crispEdges" role="img" aria-label="Polkadot Cookbook — {{RECIPE_COUNT}} recipes across 5 pathways, v{{VERSION}}">
  <!-- Generated by .claude/skills/branding/generate.sh. DO NOT EDIT. -->
  <!-- Template: templates/hero.svg.template. Tokens: .github/brand/tokens.yml. -->
  <style>
    @media (prefers-reduced-motion: reduce) {
      * { animation: none !important; }
    }
  </style>
  <rect width="1200" height="630" fill="{{CANVAS}}"/>

  <!-- B1 pink identity slab -->
  <rect x="0" y="0" width="742" height="390" fill="{{PINK}}" opacity="0">
    <animate attributeName="opacity" values="0;1" dur="{{REVEAL_DUR}}" begin="0s" fill="freeze"/>
  </rect>
  <g opacity="0">
    <animate attributeName="opacity" values="0;1" dur="{{REVEAL_DUR}}" begin="0.2s" fill="freeze"/>
    <text x="40" y="110" font-family="{{MONO}}" font-size="72" font-weight="700" fill="{{TERMINAL}}" letter-spacing="-1">POLKADOT</text>
    <text x="40" y="190" font-family="{{MONO}}" font-size="72" font-weight="700" fill="{{TERMINAL}}" letter-spacing="-1">COOKBOOK</text>
    <rect x="40" y="220" width="280" height="3" fill="{{TERMINAL}}"/>
    <text x="40" y="260" font-family="{{MONO}}" font-size="16" fill="{{TERMINAL}}" opacity="0.85">Practical, tested recipes for building on Polkadot</text>
    <text x="40" y="340" font-family="{{MONO}}" font-size="11" fill="{{TERMINAL}}" opacity="0.7">github.com/polkadot-developers/polkadot-cookbook</text>
  </g>

  <!-- B2 terminal panel: pathways list -->
  <rect x="756" y="0" width="444" height="240" fill="{{TERMINAL}}"/>
  <g opacity="0">
    <animate attributeName="opacity" values="0;1" dur="{{REVEAL_DUR}}" begin="0.5s" fill="freeze"/>
    <rect x="764" y="8" width="428" height="224" fill="none" stroke="{{CREAM}}" stroke-opacity="0.15" stroke-width="1"/>
    <text x="774" y="26" font-family="{{MONO}}" font-size="10" fill="{{CREAM}}" opacity="0.9">┌─┤ recipes/ ├──────────────────────────────────┐</text>
    <text x="774" y="52" font-family="{{MONO}}" font-size="11" fill="{{CREAM}}">│ pallets/        {{PATHWAY_PALLETS}} recipes</text>
    <text x="774" y="74" font-family="{{MONO}}" font-size="11" fill="{{CREAM}}">│ contracts/      {{PATHWAY_CONTRACTS}} recipes</text>
    <text x="774" y="96" font-family="{{MONO}}" font-size="11" fill="{{CREAM}}">│ transactions/   {{PATHWAY_TRANSACTIONS}} recipes</text>
    <text x="774" y="118" font-family="{{MONO}}" font-size="11" fill="{{CREAM}}">│ xcm/            {{PATHWAY_XCM}} recipes</text>
    <text x="774" y="140" font-family="{{MONO}}" font-size="11" fill="{{CREAM}}">│ networks/       {{PATHWAY_NETWORKS}} recipes</text>
    <text x="774" y="162" font-family="{{MONO}}" font-size="11" fill="{{CREAM}}" opacity="0.9">│ polkadot-docs/  {{DOCS_HARNESS_COUNT}} tutorials tested</text>
    <text x="774" y="196" font-family="{{MONO}}" font-size="10" fill="{{CREAM}}" opacity="0.8">│ {{RECIPE_COUNT}} recipes · {{DOCS_HARNESS_COUNT}} docs · {{WORKFLOW_COUNT}} CI</text>
    <text x="774" y="224" font-family="{{MONO}}" font-size="10" fill="{{CREAM}}" opacity="0.55">└─ rust 1.91 · zombienet · chopsticks ──────────┘</text>
  </g>

  <!-- B3 deep blue: bar chart -->
  <rect x="0" y="390" width="742" height="240" fill="{{BLUE}}" opacity="0">
    <animate attributeName="opacity" values="0;1" dur="{{REVEAL_DUR}}" begin="0.8s" fill="freeze"/>
  </rect>
  <g opacity="0">
    <animate attributeName="opacity" values="0;1" dur="{{REVEAL_DUR}}" begin="1.0s" fill="freeze"/>
    <text x="40" y="424" font-family="{{MONO}}" font-size="12" fill="{{CREAM}}" opacity="0.8" letter-spacing="2">RECIPES BY PATHWAY</text>
    <text x="40" y="460" font-family="{{MONO}}" font-size="11" fill="{{CREAM}}">PALLETS</text>
    <rect x="180" y="450" width="{{BAR_PALLETS_W}}" height="14" fill="{{PINK}}"/>
    <text x="{{BAR_PALLETS_LABEL_X}}" y="461" font-family="{{MONO}}" font-size="11" fill="{{CREAM}}" opacity="0.8">{{PATHWAY_PALLETS}}</text>
    <text x="40" y="485" font-family="{{MONO}}" font-size="11" fill="{{CREAM}}">CONTRACTS</text>
    <rect x="180" y="475" width="{{BAR_CONTRACTS_W}}" height="14" fill="{{PINK}}"/>
    <text x="{{BAR_CONTRACTS_LABEL_X}}" y="486" font-family="{{MONO}}" font-size="11" fill="{{CREAM}}" opacity="0.8">{{PATHWAY_CONTRACTS}}</text>
    <text x="40" y="510" font-family="{{MONO}}" font-size="11" fill="{{CREAM}}">TRANSACTIONS</text>
    <rect x="180" y="500" width="{{BAR_TRANSACTIONS_W}}" height="14" fill="{{PINK}}"/>
    <text x="{{BAR_TRANSACTIONS_LABEL_X}}" y="511" font-family="{{MONO}}" font-size="11" fill="{{CREAM}}" opacity="0.8">{{PATHWAY_TRANSACTIONS}}</text>
    <text x="40" y="535" font-family="{{MONO}}" font-size="11" fill="{{CREAM}}">XCM</text>
    <rect x="180" y="525" width="{{BAR_XCM_W}}" height="14" fill="{{PINK}}"/>
    <text x="{{BAR_XCM_LABEL_X}}" y="536" font-family="{{MONO}}" font-size="11" fill="{{CREAM}}" opacity="0.8">{{PATHWAY_XCM}}</text>
    <text x="40" y="560" font-family="{{MONO}}" font-size="11" fill="{{CREAM}}">NETWORKS</text>
    <rect x="180" y="550" width="{{BAR_NETWORKS_W}}" height="14" fill="{{PINK}}"/>
    <text x="{{BAR_NETWORKS_LABEL_X}}" y="561" font-family="{{MONO}}" font-size="11" fill="{{CREAM}}" opacity="0.8">{{PATHWAY_NETWORKS}}</text>
  </g>

  <!-- B4 pink accent: version badge -->
  <rect x="756" y="240" width="444" height="150" fill="{{PINK}}" opacity="0">
    <animate attributeName="opacity" values="0;1" dur="{{REVEAL_DUR}}" begin="1.2s" fill="freeze"/>
  </rect>
  <g opacity="0">
    <animate attributeName="opacity" values="0;1" dur="{{REVEAL_DUR}}" begin="1.4s" fill="freeze"/>
    <text x="778" y="288" font-family="{{MONO}}" font-size="13" fill="{{TERMINAL}}" opacity="0.8" letter-spacing="2">DOT CLI</text>
    <text x="778" y="360" font-family="{{MONO}}" font-size="56" font-weight="700" fill="{{TERMINAL}}">v{{VERSION}}</text>
  </g>

  <!-- B5 deep blue footer: KPI strip -->
  <rect x="756" y="390" width="444" height="240" fill="{{BLUE}}" opacity="0">
    <animate attributeName="opacity" values="0;1" dur="{{REVEAL_DUR}}" begin="1.6s" fill="freeze"/>
  </rect>
  <g opacity="0">
    <animate attributeName="opacity" values="0;1" dur="{{REVEAL_DUR}}" begin="1.8s" fill="freeze"/>
    <text x="778" y="430" font-family="{{MONO}}" font-size="11" fill="{{CREAM}}" opacity="0.7" letter-spacing="2">REPO STATE</text>
    <text x="778" y="475" font-family="{{MONO}}" font-size="36" font-weight="700" fill="{{CREAM}}">{{RECIPE_COUNT}}</text>
    <text x="778" y="495" font-family="{{MONO}}" font-size="10" fill="{{CREAM}}" opacity="0.7">RECIPES</text>
    <text x="878" y="475" font-family="{{MONO}}" font-size="36" font-weight="700" fill="{{CREAM}}">{{DOCS_HARNESS_COUNT}}</text>
    <text x="878" y="495" font-family="{{MONO}}" font-size="10" fill="{{CREAM}}" opacity="0.7">DOCS</text>
    <text x="988" y="475" font-family="{{MONO}}" font-size="36" font-weight="700" fill="{{CREAM}}">5</text>
    <text x="988" y="495" font-family="{{MONO}}" font-size="10" fill="{{CREAM}}" opacity="0.7">PATHS</text>
    <text x="1098" y="475" font-family="{{MONO}}" font-size="36" font-weight="700" fill="{{CREAM}}">{{WORKFLOW_COUNT}}</text>
    <text x="1098" y="495" font-family="{{MONO}}" font-size="10" fill="{{CREAM}}" opacity="0.7">CI</text>
    <text x="778" y="555" font-family="{{MONO}}" font-size="11" fill="{{CREAM}}" opacity="0.6">MIT / Apache-2.0 · rust 1.91 · polkadot-sdk pinned</text>
  </g>
</svg>
