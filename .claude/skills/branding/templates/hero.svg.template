<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 1200 400" shape-rendering="geometricPrecision"
     role="img" aria-label="Polkadot Cookbook — Practical, tested recipes for building on Polkadot.">
  <!-- Generated by .claude/skills/branding/generate.py. DO NOT EDIT.
       v2: two-panel (text + Index Mark) + paper footer. -->

  <!-- Background -->
  <rect width="1200" height="400" fill="{{CANVAS}}"/>

  <!-- Left panel (0–800): text content -->
  <text x="80" y="60" font-family="{{MONO}}" font-size="11" font-weight="600"
        fill="{{PINK}}" letter-spacing="2">&#9671; THE POLKADOT COOKBOOK · v{{VERSION}}</text>

  <text x="80" y="148" font-family="{{MONO}}" font-size="72" font-weight="700"
        fill="{{FG}}" letter-spacing="-3">Practical,</text>
  <text x="80" y="228" font-family="{{MONO}}" font-size="72" font-weight="700"
        fill="{{FG}}" letter-spacing="-3">tested recipes.</text>

  <!-- Accent rule -->
  <rect x="80" y="248" width="80" height="4" fill="{{PINK}}"/>

  <!-- Lede -->
  <text x="80" y="284" font-family="{{MONO}}" font-size="14" fill="{{FG_MUTED}}">Build runtime logic, smart contracts, dApps, and</text>
  <text x="80" y="302" font-family="{{MONO}}" font-size="14" fill="{{FG_MUTED}}">cross-chain applications with working code examples.</text>

  <!-- CTA box -->
  <rect x="80" y="320" width="340" height="24" fill="{{SURFACE}}" stroke="{{LINE}}" stroke-width="1"/>
  <text x="90" y="337" font-family="{{MONO}}" font-size="13" fill="{{PINK}}" font-weight="700">&#9656;</text>
  <text x="104" y="337" font-family="{{MONO}}" font-size="13" fill="{{FG}}">dot create --title my-parachain</text>

  <!-- Right panel (800–1200): surface bg -->
  <rect x="800" y="0" width="400" height="352" fill="{{SURFACE}}"/>
  <line x1="800" y1="0" x2="800" y2="352" stroke="{{LINE}}" stroke-width="1"/>

  <!-- Index Mark (centered in right panel) -->
  <g transform="translate(900, 40)">
    <rect x="0" y="0" width="200" height="200" fill="none" stroke="{{FG}}" stroke-width="3"/>
    <rect x="0" y="0" width="200" height="28" fill="{{PINK}}"/>
    <rect x="172" y="60" width="28" height="60" fill="{{PINK}}"/>
    <rect x="18" y="48" width="140" height="4" fill="{{FG}}"/>
    <rect x="18" y="68" width="110" height="4" fill="{{FG}}" opacity=".7"/>
    <rect x="18" y="88" width="128" height="4" fill="{{FG}}" opacity=".7"/>
    <rect x="18" y="108" width="90" height="4" fill="{{FG}}" opacity=".5"/>
    <rect x="18" y="128" width="112" height="4" fill="{{FG}}" opacity=".5"/>
    <rect x="18" y="148" width="56" height="4" fill="{{FG}}" opacity=".35"/>
  </g>

  <!-- KPI strip -->
  <line x1="800" y1="260" x2="1200" y2="260" stroke="{{LINE}}" stroke-width="1"/>

  <text x="850" y="300" font-family="{{MONO}}" font-size="36" font-weight="700"
        fill="{{PINK}}" letter-spacing="-1">{{RECIPE_COUNT}}</text>
  <text x="850" y="320" font-family="{{MONO}}" font-size="9" fill="{{FG_DIM}}"
        letter-spacing="1.5">RECIPES</text>

  <line x1="933" y1="270" x2="933" y2="340" stroke="{{LINE}}" stroke-width="1"/>

  <text x="960" y="300" font-family="{{MONO}}" font-size="36" font-weight="700"
        fill="{{PINK}}" letter-spacing="-1">{{PATHWAY_COUNT}}</text>
  <text x="960" y="320" font-family="{{MONO}}" font-size="9" fill="{{FG_DIM}}"
        letter-spacing="1.5">PATHWAYS</text>

  <line x1="1060" y1="270" x2="1060" y2="340" stroke="{{LINE}}" stroke-width="1"/>

  <text x="1080" y="300" font-family="{{MONO}}" font-size="36" font-weight="700"
        fill="{{PINK}}" letter-spacing="-1">{{WORKFLOW_COUNT}}</text>
  <text x="1080" y="320" font-family="{{MONO}}" font-size="9" fill="{{FG_DIM}}"
        letter-spacing="1.5">WORKFLOWS</text>

  <!-- Footer bar -->
  <rect x="0" y="352" width="1200" height="48" fill="{{PAPER}}"/>
  <text x="80" y="380" font-family="{{MONO}}" font-size="10" fill="{{INK}}"
        letter-spacing="2" opacity=".85">PALLETS · CONTRACTS · TRANSACTIONS · XCM · NETWORKS</text>
  <text x="1120" y="380" font-family="{{MONO}}" font-size="10" fill="{{INK}}"
        text-anchor="end" opacity=".6" letter-spacing="1">github.com/polkadot-developers/polkadot-cookbook</text>
  <text x="1120" y="394" font-family="{{MONO}}" font-size="9" fill="{{INK}}"
        text-anchor="end" opacity=".5">MIT / Apache-2.0</text>
</svg>
