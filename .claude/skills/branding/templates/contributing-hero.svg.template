<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 1200 400" shape-rendering="geometricPrecision" role="img" aria-label="Contributing to Polkadot Cookbook">
  <!-- Generated. DO NOT EDIT. Strict 3-color contributing hero. -->
  <rect width="1200" height="400" fill="{{CANVAS}}"/>

  <!-- Pink slab -->
  <rect x="0" y="0" width="460" height="400" fill="{{PINK}}"/>
  <text x="40" y="130" font-family="{{MONO}}" font-size="44" font-weight="700" fill="{{BLACK}}" letter-spacing="-1">HOW TO</text>
  <text x="40" y="180" font-family="{{MONO}}" font-size="44" font-weight="700" fill="{{BLACK}}" letter-spacing="-1">CONTRIBUTE</text>
  <rect x="40" y="200" width="260" height="3" fill="{{BLACK}}"/>
  <text x="40" y="240" font-family="{{MONO}}" font-size="14" fill="{{BLACK}}" opacity="0.85">Ship a recipe. Fix a test.</text>
  <text x="40" y="262" font-family="{{MONO}}" font-size="14" fill="{{BLACK}}" opacity="0.85">Improve a pathway.</text>

  <!-- Accent panel with steps -->
  <rect x="460" y="0" width="740" height="400" fill="{{ACCENT_PANEL}}"/>
  <rect x="474" y="14" width="712" height="372" fill="none" stroke="{{FG}}" stroke-opacity="0.15"/>
  <text x="490" y="40" font-family="{{MONO}}" font-size="11" fill="{{FG}}" opacity="0.9">┌─┤ contributing steps ├──────────────────────────────┐</text>
  <g font-family="{{MONO}}" font-size="13" fill="{{FG}}">
    <text x="490" y="80">│ 01  fork + branch (never work on master)</text>
    <text x="490" y="108">│ 02  pick a skill: /add-recipe or /add-polkadot-docs-test</text>
    <text x="490" y="136">│ 03  run npm ci &amp;&amp; npm test in the harness</text>
    <text x="490" y="164">│ 04  run cargo fmt &amp;&amp; cargo clippy -- -D warnings</text>
    <text x="490" y="192">│ 05  update versions.yml if pinning new upstream</text>
    <text x="490" y="220">│ 06  open a DRAFT pr with completed test-plan checklist</text>
    <text x="490" y="248">│ 07  on merge: release skill auto-bumps + cuts cover</text>
  </g>
  <rect x="490" y="276" width="680" height="1" fill="{{FG}}" opacity="0.2"/>
  <text x="490" y="304" font-family="{{MONO}}" font-size="11" fill="{{FG}}" opacity="0.6">│ full guide: CONTRIBUTING.md</text>
  <text x="490" y="326" font-family="{{MONO}}" font-size="11" fill="{{FG}}" opacity="0.6">│ brand system: .github/brand/DESIGN.md</text>
  <text x="490" y="348" font-family="{{MONO}}" font-size="11" fill="{{FG}}" opacity="0.6">│ license: MIT / Apache-2.0</text>
  <text x="490" y="370" font-family="{{MONO}}" font-size="11" fill="{{FG}}" opacity="0.55">└─────────────────────────────────────────────────────┘</text>
</svg>
