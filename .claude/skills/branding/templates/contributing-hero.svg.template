<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 1200 400" shape-rendering="geometricPrecision"
     role="img" aria-label="How to contribute to the Polkadot Cookbook">
  <!-- Generated by .claude/skills/branding/generate.py. DO NOT EDIT.
       v2: 3-step horizontal flow + right KPI column. -->

  <rect width="1200" height="400" fill="{{CANVAS}}"/>

  <!-- Step 01 -->
  <rect x="60" y="80" width="280" height="200" fill="{{SURFACE}}" stroke="{{LINE}}" stroke-width="1"/>
  <text x="84" y="130" font-family="{{MONO}}" font-size="28" font-weight="800" fill="{{PINK}}" letter-spacing="-1">01</text>
  <text x="84" y="168" font-family="{{MONO}}" font-size="20" font-weight="700" fill="{{FG}}" letter-spacing="-.5">Develop</text>
  <text x="84" y="194" font-family="{{MONO}}" font-size="11" fill="{{FG_MUTED}}" letter-spacing="1">Fork + branch</text>
  <text x="84" y="214" font-family="{{MONO}}" font-size="11" fill="{{FG_DIM}}" letter-spacing="1">never work on master</text>

  <!-- Arrow 1-2 -->
  <text x="360" y="190" font-family="{{MONO}}" font-size="24" fill="{{PINK}}">&#8594;</text>

  <!-- Step 02 -->
  <rect x="400" y="80" width="280" height="200" fill="{{SURFACE}}" stroke="{{LINE}}" stroke-width="1"/>
  <text x="424" y="130" font-family="{{MONO}}" font-size="28" font-weight="800" fill="{{PINK}}" letter-spacing="-1">02</text>
  <text x="424" y="168" font-family="{{MONO}}" font-size="20" font-weight="700" fill="{{FG}}" letter-spacing="-.5">Harness</text>
  <text x="424" y="194" font-family="{{MONO}}" font-size="11" fill="{{FG_MUTED}}" letter-spacing="1">/add-recipe or</text>
  <text x="424" y="214" font-family="{{MONO}}" font-size="11" fill="{{FG_MUTED}}" letter-spacing="1">/add-polkadot-docs-test</text>

  <!-- Arrow 2-3 -->
  <text x="700" y="190" font-family="{{MONO}}" font-size="24" fill="{{PINK}}">&#8594;</text>

  <!-- Step 03 -->
  <rect x="740" y="80" width="280" height="200" fill="{{SURFACE}}" stroke="{{LINE}}" stroke-width="1"/>
  <text x="764" y="130" font-family="{{MONO}}" font-size="28" font-weight="800" fill="{{PINK}}" letter-spacing="-1">03</text>
  <text x="764" y="168" font-family="{{MONO}}" font-size="20" font-weight="700" fill="{{FG}}" letter-spacing="-.5">PR</text>
  <text x="764" y="194" font-family="{{MONO}}" font-size="11" fill="{{FG_MUTED}}" letter-spacing="1">Open a draft PR with</text>
  <text x="764" y="214" font-family="{{MONO}}" font-size="11" fill="{{FG_MUTED}}" letter-spacing="1">completed test-plan</text>

  <!-- Footer -->
  <text x="60" y="360" font-family="{{MONO}}" font-size="10" fill="{{FG_DIM}}" letter-spacing="1.5">CONTRIBUTING.md · .github/brand/DESIGN.md · MIT / Apache-2.0</text>
</svg>
